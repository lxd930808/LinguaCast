# secrets/

Put deployment-only secret files here (mode 600). In Apple mode the account service
reads the Sign in with Apple private key from `apple-sign-in-key.p8`. Everything in
this directory except this README is ignored by git.
